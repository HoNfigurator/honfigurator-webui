// components/ServerDropdown.js
import { Menu } from 'antd';

const ServerDropdown = ({ serverOptions, handleServerChange, handleEditServer, handleRemoveServer, setAddServerModalVisible }) => {
  // Your dropdown menu JSX code here...
};

export default ServerDropdown;